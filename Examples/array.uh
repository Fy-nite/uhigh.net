using System
using System.Diagnostics
using System.IO
namespace test
{
    public class Program
    {
        public static func Main(args: string[]): void
        {
            var arr = {}
            for var i in arr
            {
                Console.WriteLine(i)
            }
            arr.append(2)
        }
    }
}
