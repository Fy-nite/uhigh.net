{
    "version": "0.2.0",
    "configurations": [
        {
            "name": ".NET Launch μHigh Compiler",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "build", // optional, if you have a build task
            "program": "${workspaceFolder}/bin/Debug/net8.0/uhigh.dll", // adjust for your output path and target framework
            "args": [
                // Add any command-line arguments you want to test here, e.g.:
                "testing/main.uh"
            ],
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "stopAtEntry": false
        }
    ]
}